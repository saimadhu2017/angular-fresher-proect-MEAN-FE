import { SearchByProductPipe } from './search-by-product.pipe';

describe('SearchByProductPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchByProductPipe();
    expect(pipe).toBeTruthy();
  });
});
